meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "username": "shreshth",
    "password": "thisone"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
