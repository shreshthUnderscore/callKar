meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "shreshth",
    "password": "thisone"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
